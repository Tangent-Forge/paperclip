export function Sources() {
  return (
    <div className="text-sm text-muted-foreground">
      TODO: list gbrain sources with page/chunk counts and freshness (last sync, stale-page count).
    </div>
  );
}
