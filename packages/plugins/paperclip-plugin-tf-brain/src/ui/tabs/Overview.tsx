export function Overview() {
  return (
    <div className="text-sm text-muted-foreground">
      TODO: metric tiles for pages, chunks, sources, embed coverage, brain score, orphans, stale pages, recent syncs.
    </div>
  );
}
