export function Audit() {
  return (
    <div className="text-sm text-muted-foreground">
      TODO: render gbrain doctor health-check results (status per check, last run, fix hints).
    </div>
  );
}
