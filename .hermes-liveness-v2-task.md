Implement the production-quality Paperclip liveness sentinel v2 in this worktree. Do not commit, push, deploy, migrate any live DB, or mutate external systems.

Context
- Current branch has pure GET/read projection fix in e026fbd20 and new DB schema/migration 0104 in 859006424.
- Ignore commits 4506a4d4d and 2fbbecb97: the former was a bad scaffold and the latter fully reverted it.
- Read /home/tfhub/artifacts/paperclip-liveness-sentinel-audit/IMPLEMENTATION-PLAN.md.
- Existing implementation is in server/src/services/recovery/service.ts around collectIssueGraphLivenessFindings(), createIssueGraphLivenessEscalation(), and reconcileIssueGraphLiveness(). Existing classifier is recovery/issue-graph-liveness.ts.
- DB exports livenessIncidents, livenessReconcileRuns, livenessEffectOutbox; issues has livenessIncidentId.

Required implementation
1. Pure observer
- Extract the existing complete read-only collection/query/classification path into server/src/services/recovery/liveness-observer.ts. It must preserve all current relation, agent, active run, wake, interaction, approval, recovery issue/action context; do not pass empty arrays.
- Observer must not import issueService, issueRecoveryActionService, wake enqueue, logActivity, or mutation helpers.
- Exclude all Object.values(RECOVERY_ORIGIN_KINDS) from candidates before classification (handle null origin_kind correctly; SQL NOT IN excludes null, so explicitly allow null).
- Return findings with canonical source identity. Plugin/external: originKind namespace + immutable originId. Native: provider paperclip:issue + issue.id. Canonical incident class issue_graph_liveness. Deduplicate multiple dependents by canonical recovery leaf/source identity, deterministically retaining the most severe/lexical first evidence.

2. Durable reconciler in focused module server/src/services/recovery/liveness-v2.ts
- Wire recoveryService.reconcileIssueGraphLiveness to use this; leave preview observer-only. Remove/disable any path that creates a fresh ticket after terminal. Legacy mutator helpers can remain temporarily only if unreachable from reconcile path; do not layer two emitters.
- Run per company under pg_advisory_xact_lock; one running row in liveness_reconcile_runs, exactly one terminal disposition for completion. Because a failed transaction rolls back, record failed run disposition in a separate transaction/catch after rollback.
- Hysteresis defaults: 2 present observations >=5m apart activates; 2 absent observations >=5m apart clears; recurrence only after >=30m cooldown; max 10 activations/company/run; max 50 pending effects/company. Allow deterministic injected now/options for tests with validated bounded values.
- Permanent atomic upsert on company/provider/originId/incidentClass. First seen state observed generation 1. Activation queues open_or_reopen_sentinel and sync_blocker in same transaction. Stable state queues nothing. Clear after 2 absent observations queues close_sentinel, no new incident/sentinel. Recurrence reuses incident, increments generation once, queues new generation effects. Clear absent handling must inspect existing non-suppressed incidents not in current findings.
- Recovery-on-recovery source/recovery leaf never activates and never emits effects.
- Backpressure does not mint an aggregate ticket; record bounded run/result counts and leave incident observed/suppressed without queueing over cap.
- Concurrency exactly-once via advisory lock + unique constraints.

3. Idempotent outbox worker
- Add claim using transaction + FOR UPDATE SKIP LOCKED or equivalent atomic update/returning.
- Runtime effect application fail-closed unless PAPERCLIP_LIVENESS_V2_CANARY_COMPANY_ID exactly equals incident.companyId. No broad/global enable.
- open_or_reopen_sentinel: find by permanent livenessIncidentId, create at most one harness_liveness_escalation issue if none; originId=incident UUID; originFingerprint=sentinel:v2:<sha256 of stable canonical identity JSON>; if existing terminal, update/reopen same issue ID. Use issuesService as needed outside the transaction but make retry idempotent through permanent DB constraints/query.
- sync_blocker: at most one equivalent blocker relation; close_sentinel: close same sentinel and remove its blocker relationship; enqueue_wake only after sentinel remains nonterminal and use idempotency key incident+generation+effect. Never post-finalization wake.
- Effect retries must not duplicate issues/comments/relations/wakes/activities. On failure increment attempts and either pending with bounded backoff or failed after a small max; sanitize errors.

4. Tests
- Invert the existing test that expected a fresh sentinel after terminal: assert same incident ID, same sentinel issue ID, generation increment, no second issue.
- Add focused tests for canonical provider namespace collisions, native identity, all recovery-origin exclusions, 32 concurrent reconciliation calls, repeated/stable idempotency, flapping below threshold, clear+recur same sentinel, exactly one terminal run disposition, backpressure, and canary company fail-closed effect gate. Use deterministic time and injected thresholds.
- Preserve existing behavior outside this subsystem and existing test coverage.

5. Verification
- Build @paperclipai/provider-router-local first if needed.
- Run focused Vitest files using paths relative to server package root, server typecheck, DB liveness tests, migration numbering check.
- Keep worktree clean except intended source/tests and this prompt file; do not stage prompt file.

Work carefully. Inspect existing service types and test helpers rather than guessing APIs. Finish with a concise summary of files and actual test output.