{
  "mcpServers": {
    "gumroad": {
      "command": "node",
      "args": [
        "packages/mcp-server-gumroad/dist/stdio.js"
      ],
      "env": {
        "GUMROAD_ACCESS_TOKEN": "${GUMROAD_ACCESS_TOKEN}"
      }
    }
  }
}
